[package]
name = "tests"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
libraw_r = { workspace = true }

[dev-dependencies]
libraw_r = { workspace = true }
criterion = { version = "0.4.0", features = ["html_reports"] }

[[bench]]
name = "libraw"
harness = false
